// components/Loading/Loading.tsx

'use client';

import React, { FC } from 'react';

const Loading: FC = () => {
	return (
		<h1 className="text-2xl font-bold mb-4 justify-self-center">Loading...</h1>
	);
};

export default Loading;
